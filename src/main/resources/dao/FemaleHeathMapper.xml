<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="dao.FemaleHealthMapper">
    <select id="getFemaleHealthsByStuId" parameterType="long" resultType="domain.FemaleHealth">
        SELECT * from female_health where student_id=#{id}
    </select>
</mapper>